/*

// initializers
 All are passed into the initializer, so the user of the class must know all three values at initialization time. This is another important safety and design feature of Swift. What’s the use of a treasure without a latitude value, for example? It needs to have all three values to exist

// compute properties 

// access control

*/